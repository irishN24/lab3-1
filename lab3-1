//Реализовать сортировку массива методом прямая вставка
#include <stdio.h>

int main() {
	int temp = 0;
	int numbers[] = {15, 4, 1, 9, 16, 2, 13, 18, 14, 4};
	size_t count = sizeof(numbers) / sizeof(numbers[0]);
	for (int i = 1; i <= count; i++) {
		if (numbers[i]
	}




	return 0;
}
